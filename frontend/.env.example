# MSRTC NextGen — Frontend Environment
# Copy to .env and fill in values

# API base URL — points to the backend server
# In development, Vite proxies /api/v1 to this address
VITE_API_URL=http://localhost:3000
